audio_play_sound(snd_lvl_complete, 0, false);

//TODO: Fix the external sprite drawing code.
//draw_sprite_ext(spr_splash_text, 0, display_get_gui_width(), display_get_gui_height(), 1, 1, 0, c_white, 1);