  {
    var obj_inst; //detects instances of fade_trans object.
    var obj_inst = instance_create(0,0, obj_fade_trans);
    audio_stop_sound(snd_bg_level);
  }
  //The ideal scenario is to go back to the main menu after dying somehow.
